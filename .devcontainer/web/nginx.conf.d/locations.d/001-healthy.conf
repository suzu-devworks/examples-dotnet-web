# health check endpoint
location ~ ^/(health|healthz)$ {
    access_log off;
    allow 127.0.0.1;      # ローカルホストからのアクセスを許可
    # allow 10.0.0.0/8;   # 必要に応じてLBのIPなどを許可
    deny all;           # それ以外を拒否

    add_header Content-Type text/plain;
    return 200 "ok";
}
