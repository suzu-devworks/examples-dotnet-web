{
  "Logging": {
    "LogLevel": {
      "Default": "Trace"
    }
  },
  "NLog": {
    "rules": [
      // Output debug log
      {
        "logger": "*",
        "minLevel": "Trace",
        "writeTo": "debuglogfile"
      }
    ]
  },
  "AllowedHosts": "*"
}
